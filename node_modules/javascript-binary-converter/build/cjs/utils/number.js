"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.normalizeBigInt = exports.getClosestDividable = exports.isBigInt = exports.isFloat = exports.isNumeric = void 0;
function isNumeric(n) {
    return !isNaN(parseFloat(n)) && isFinite(n);
}
exports.isNumeric = isNumeric;
function isFloat(number) {
    return typeof number === 'number' && !Number.isInteger(number);
}
exports.isFloat = isFloat;
function isBigInt(number) {
    return typeof number === 'bigint' || (number > 4294967295 || number < -2147483647);
}
exports.isBigInt = isBigInt;
function getClosestDividable(divided, divisor) {
    let modulo = divided % divisor;
    while (modulo !== 0) {
        divided++;
        modulo = divided % divisor;
    }
    return divided;
}
exports.getClosestDividable = getClosestDividable;
function normalizeBigInt(decimal, nBits = BigInt(64)) {
    [decimal, nBits] = [BigInt(decimal), BigInt(nBits)];
    if (decimal >= 0)
        return decimal;
    return (BigInt(BigInt(2) ** nBits) + decimal);
}
exports.normalizeBigInt = normalizeBigInt;
//# sourceMappingURL=number.js.map