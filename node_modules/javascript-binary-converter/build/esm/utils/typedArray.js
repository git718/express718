export function typedArrayToIntegers(typedArray) {
    const decimals = [];
    for (let decimal of typedArray) {
        decimals.push(decimal);
    }
    return decimals;
}
//# sourceMappingURL=typedArray.js.map